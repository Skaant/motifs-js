import { MODULE } from "../../spec-section/_enums/type/spec-section.type.enum.js";

export default ({
  type: MODULE,
  /** We should test that every GLOBAL GETR
   * has been correctly called using while
   * executing `init.js`. */
  group: []
})